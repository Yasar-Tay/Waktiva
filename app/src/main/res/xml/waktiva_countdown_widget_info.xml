<?xml version="1.0" encoding="utf-8"?>
<!--
  The countdown widget: a full-width 4×1 bar with the time left to the next prayer at the bar's
  full height. It can be narrowed or made taller; the countdown grows with the height. Refreshed
  with the main widget by WaktivaWidget.updateAll at prayer boundaries, so updatePeriodMillis
  stays 0.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_countdown_description"
    android:initialLayout="@layout/widget_countdown_bar"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="40dp"
    android:previewLayout="@layout/widget_countdown_bar_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
