<?xml version="1.0" encoding="utf-8"?>
<!--
  The countdown widget: a small 2×1 tile with the time left to the next prayer as large as it
  fits. It resizes freely; the countdown grows with the cell. Refreshed with the main widget by
  WaktivaWidget.updateAll at prayer boundaries, so updatePeriodMillis stays 0.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_countdown_description"
    android:initialLayout="@layout/widget_countdown_small"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:previewLayout="@layout/widget_countdown_small_preview"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
